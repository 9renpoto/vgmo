{
  "name": "vgmo",
  "private": true,
  "author": "9renpoto",
  "scripts": {
    "storybook": "npm run storybook -w @vgmo/ui",
    "build-storybook": " npm run build-storybook -w @vgmo/ui",
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces",
    "crawl": "npm run crawl -w @vgmo/crawler"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "services/*"
    ]
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.5",
    "@types/node": "^20.12.12",
    "@typescript/native-preview": "7.0.0-dev.20251013.1",
    "c8": "^10.1.2"
  }
}
