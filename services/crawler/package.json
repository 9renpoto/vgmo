{
  "name": "@vgmo/crawler",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf lib && tsgo --build",
    "crawl": "node --experimental-strip-types src/main.ts && npx biome format --write ../web/public/data/concerts.json",
    "test": "c8 --reporter=lcov node --test --experimental-strip-types --test-reporter=junit --test-reporter-destination=junit-crawler.xml tests/**/*.test.ts"
  },
  "author": "9renpoto",
  "dependencies": {
    "@vgmo/types": "0.0.1",
    "cheerio": "^1.2.0",
    "feedparser": "^2.2.10",
    "undici": "^7.21.0"
  }
}
