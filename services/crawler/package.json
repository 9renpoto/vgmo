{
  "name": "@vgmo/crawler",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf lib && tsgo --build",
    "crawl": "node --experimental-strip-types src/main.ts",
    "test": "c8 --reporter=lcov node --test --experimental-strip-types --test-reporter=junit --test-reporter-destination=junit-crawler.xml tests/**/*.test.ts"
  },
  "author": "9renpoto",
  "dependencies": {
    "feedparser": "^2.2.10",
    "@vgmo/types": "0.0.1",
    "cheerio": "^1.0.0-rc.12"
  }
}
