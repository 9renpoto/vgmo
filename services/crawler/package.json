{
  "name": "@vgmo/crawler",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "test": "node --test --experimental-strip-types tests/**/*.test.ts",
    "coverage": "c8 npm test && c8 report --reporter=lcov"
  },
  "author": "9renpoto"
}
